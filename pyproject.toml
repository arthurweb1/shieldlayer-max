[project]
name = "shieldlayer-max"
version = "1.0.0"
requires-python = ">=3.11"
description = "Enterprise AI Gateway with EU AI Act compliance enforcement"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.mypy]
python_version = "3.11"
strict = true
